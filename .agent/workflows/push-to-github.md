---
description: How to push changes to GitHub
---
// turbo-all
1. Add all changes to git:
   ```bash
   git add .
   ```

2. Commit the changes:
   ```bash
   git commit -m "Update QA dashboard and Login page"
   ```

3. Push to the main branch:
   ```bash
   git push origin main
   ```
